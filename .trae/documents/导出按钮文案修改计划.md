# 导出按钮文案修改计划

## Summary

将统计栏中"导出"按钮的文案从"导出"改为"Excle导出"。仅修改该按钮的显示文本，不涉及任何样式或交互逻辑变更。

## Current State Analysis

- 导出按钮位于 `src/pages/JobList/index.tsx` 统计栏（`stats-section`）区域
- 当前代码：`<Button icon={<DownloadOutlined />} className="export-btn">导出</Button>`
- 该按钮仅有展示作用，未绑定实际的导出逻辑（当前为占位按钮）

## Proposed Changes

### `src/pages/JobList/index.tsx` — 修改按钮文案

将导出按钮的文本内容从"导出"替换为"Excle导出"：

```tsx
<Button icon={<DownloadOutlined />} className="export-btn">
  Excle导出
</Button>
```

## Assumptions & Decisions

1. **拼写确认**：已与用户确认，按原文"Excle导出"修改（非"Excel导出"）。
2. **影响范围**：仅修改文案字符串，不改动按钮样式、图标、className 或任何交互行为。

## Verification Steps

1. 刷新页面，查看统计栏右侧的导出按钮
2. 确认按钮文案显示为"Excle导出"
